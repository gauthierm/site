{
  "name": "@silverorange/site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check-if-pretty": "prettier --list-different 'Site/**/*.php'",
    "make-pretty": "prettier --write 'Site/**/*.php'"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.10.2",
    "prettier": "^1.16.4"
  }
}
